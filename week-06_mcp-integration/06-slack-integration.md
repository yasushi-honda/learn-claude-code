# Lesson 6: Slack連携

> 対応する公式ドキュメント: [Claude Code in Slack](https://code.claude.com/docs/en/slack)

## 学習目標

- Claude Code in Slackの概要と仕組みを説明できる
- Slackから`@Claude`メンションでコーディングタスクを委譲できる
- セットアップ手順（Claudeアプリのインストール・アカウント接続・GitHub連携）を実行できる
- ルーティングモード（Code only / Code + Chat）を使い分けられる
- バグ報告からPR作成までのフローを理解できる
- セキュリティとアクセス制御の考慮事項を説明できる

## 概要

Claude Code in Slackは、SlackワークスペースからClaude Codeのパワーを直接活用できる機能です。`@Claude`メンションでコーディングタスクを送ると、Claudeが自動的にコーディングの意図を検出し、Web上のClaude Codeセッションを作成してタスクを委譲します。チームの会話からシームレスに開発作業を開始できます。

この連携は既存の「Claude for Slack」アプリをベースに構築されており、コーディング関連のリクエストをWeb上のClaude Codeにインテリジェントにルーティングする機能を追加しています。

## 本文

### 主なユースケース

#### バグ調査と修正

バグがSlackチャンネルで報告されたらすぐにClaudeに調査・修正を依頼できます：

```text
@Claude エラーログに「TypeError: Cannot read property 'id' of undefined」が出ています。
auth.tsのfindUserById関数を調査して修正してください
```

#### クイックコードレビューと修正

チームのフィードバックを元に、小さな機能追加やリファクタリングを依頼：

```text
@Claude チームのレビューで指摘された認証フォームの入力バリデーションを強化してください。
具体的には空白文字のみのメールアドレスをはじくようにしてほしいです
```

#### 協調デバッグ

スレッドの会話コンテキスト（エラー再現手順・ユーザーレポートなど）をClaudeが自動取得してデバッグに活用します：

```text
# スレッド内に既にエラー情報や再現手順がある場合
@Claude このスレッドの情報を使って原因を特定して修正してください
```

#### 並列タスク実行

Slackでタスクを起動して他の作業を続け、完了通知を受け取る：

```text
@Claude 新しい通知システムのユニットテストを書いてください。
完了したら教えてください
```

### 前提条件

| 要件 | 詳細 |
|------|------|
| Claudeプラン | Pro・Max・Teams・Enterprise（Claude Codeアクセス付き） |
| Web版Claude Code | claude.ai/codeへのアクセスが必要 |
| GitHubアカウント | Web版Claude Codeに接続し、少なくとも1つのリポジトリを認証済み |
| Slack認証 | SlackアカウントをClaudeアカウントに連携済み |

> **重要:** Claude Code in SlackはGitHubのリポジトリのみ対応しています（GitLabなどは現在未対応）。

### セットアップ手順

#### ステップ1: Slack App MarketplaceからClaude appをインストール

ワークスペースの管理者がSlack App MarketplaceからClaudeアプリをインストールします：

```
https://slack.com/marketplace/A08SF47R6P4
```

「Add to Slack」をクリックしてインストールを開始します。

> **注意:** Slackワークスペースへのアプリインストールには管理者権限が必要です。

#### ステップ2: Claudeアカウントを接続する

アプリのインストール後、各ユーザーが個人のClaudeアカウントを認証します：

1. Slackの「Apps」セクションで「Claude」をクリック
2. 「App Home」タブに移動
3. 「Connect」をクリックしてSlackアカウントとClaudeアカウントを連携
4. ブラウザで認証フローを完了

#### ステップ3: Web版Claude Codeを設定する

1. [claude.ai/code](https://claude.ai/code)にSlackと同じアカウントでサインイン
2. GitHubアカウントを接続（未接続の場合）
3. 作業対象のリポジトリを少なくとも1つ認証

#### ステップ4: ルーティングモードを選択する

Claude App HomeでRouting Modeを設定します：

| モード | 動作 | 推奨シーン |
|--------|------|----------|
| **Code only** | すべての@メンションをClaude Codeセッションにルーティング | Slackを開発タスク専用に使うチーム |
| **Code + Chat** | メッセージを分析してClaude Code（コーディング）またはClaude Chat（質問・分析・執筆）に振り分け | Slackを様々な用途に使うチーム |

> **ヒント:** Code + Chatモードで誤ったルーティングが行われた場合：
> - ChatにルーティングされたがCodeが必要 → 「Retry as Code」をクリック
> - CodeにルーティングされたがChatが必要 → スレッドでChat選択

#### ステップ5: チャンネルにClaudeを追加する

インストール後、Claudeは自動的にどのチャンネルにも追加されません。使用したいチャンネルで：

```
/invite @Claude
```

> **重要:** Claudeは招待されたチャンネルの@メンションにのみ応答します。ダイレクトメッセージ（DM）では動作しません。

### 動作の仕組み

#### 自動検出

`@Claude`をメンションすると、Claudeがメッセージを分析してコーディングタスクかどうかを判定します：

```
┌─────────────────────────────────────────────────┐
│              Slack Channel/Thread               │
│                                                 │
│  User: @Claude バグを修正して                    │
│                ↓ コーディング意図を検出           │
│  Claude: Claude Codeセッションを作成中...         │
│           ↓                                     │
│  Claude: 作業中 (進捗更新)...                    │
│           ↓                                     │
│  Claude: 完了! [View Session] [Create PR]        │
└─────────────────────────────────────────────────┘
```

#### コンテキスト収集

**スレッド内でメンション**: スレッド内のすべてのメッセージからコンテキストを収集します。バグ報告スレッドに詳細があればClaudeが自動的に参照します。

**チャンネルで直接メンション**: チャンネルの最近のメッセージから関連コンテキストを取得します。

> **セキュリティ注意:** @Claudeを呼び出すと、Claudeは会話コンテキストにアクセスします。コンテキスト内の他のメッセージの指示に従う場合があるため、信頼できるSlack会話でのみ使用してください。

#### セッションフロー

```
1. @Claude でコーディングリクエストをメンション
2. Claudeがコーディング意図を検出
3. claude.ai/code上に新しいClaude Codeセッションを作成
4. 作業進捗がSlackスレッドに投稿
5. 完了時: @メンションで要約とアクションボタンを通知
6. [View Session]または[Create PR]でアクションを実行
```

### UIとアクションボタン

#### セッション完了後のアクション

| ボタン | 機能 |
|--------|------|
| **View Session** | ブラウザでClaude Codeセッションの全履歴を表示。続きの指示も可能 |
| **Create PR** | セッションの変更からプルリクエストを直接作成 |
| **Retry as Code** | Chatとして応答された場合にClaude Codeセッションとして再試行 |
| **Change Repo** | Claudeが誤ったリポジトリを選択した場合に変更 |

#### リポジトリ選択

Claudeはslackの会話コンテキストから自動的にリポジトリを選択します。複数のリポジトリが当てはまる場合、ドロップダウンで選択できます。

### 具体的なワークフロー例

#### バグ報告 → PR作成フロー

```
# ステップ1: バグがチャンネルで報告される
ユーザーA: 本番環境でログイン時に500エラーが出ています
           ログ: "UnhandledPromiseRejection: db timeout"

# ステップ2: Claudeに委譲
ユーザーB: @Claude このスレッドを見て、DBタイムアウトの問題を調査して修正してください

# ステップ3: Claudeが作業
Claude: 🔍 リポジトリを分析しています...
Claude: 📝 database.ts のconnectionPool設定を確認中...
Claude: ✅ 修正完了! タイムアウト設定を30秒に延長しました

# ステップ4: PRを作成
Claude: 作業が完了しました。[View Session] [Create PR]
→ [Create PR]クリックでPRが自動作成
```

#### 機能実装 → レビュー依頼

```
@Claude payments/stripe.tsに支払い失敗時のリトライロジックを追加してください。
最大3回リトライし、指数バックオフを使用してください。
完了したらテストも書いてください
```

### アクセスとパーミッション管理

#### ユーザーレベルのアクセス

| アクセス種別 | 要件 |
|------------|------|
| Claude Codeセッション | 各ユーザーが自分のClaudeアカウントでセッションを実行 |
| 使用量・レート制限 | 個人プランの制限に対してカウント |
| リポジトリアクセス | 個人がWeb版Claude Codeに接続したリポジトリのみ |
| セッション履歴 | claude.ai/codeのClaude Code履歴に表示 |

#### ワークスペースレベルの管理

| 制御 | 説明 |
|------|------|
| アプリインストール | ワークスペース管理者がSlack App MarketplaceからClaudeアプリの導入を決定 |
| Enterprise Grid | 組織管理者がどのワークスペースがアクセスできるか制御 |
| アプリ削除 | ワークスペースからアプリを削除すると全ユーザーのアクセスが即座に無効化 |

#### チャンネルベースのアクセス制御

チャンネルへの招待によってアクセスを細かく制御できます：

```
# Claudeを特定チャンネルに追加
/invite @Claude

# 開発チームのみのプライベートチャンネルを活用
# → #dev-claudeなどの専用チャンネルでアクセスを制限
```

- **招待が必要**: Claudeが追加されたチャンネルでのみ動作
- **プライベートチャンネル対応**: パブリック・プライベート両方のチャンネルで利用可能
- **管理者によるゲーティング**: Claudeを招待するチャンネルを管理することで、使用できるユーザーを制限できる

### 効果的なSlack利用のベストプラクティス

#### 具体的なリクエストの書き方

```text
# 良い例: 具体的
@Claude src/auth/login.ts のhandleLoginError関数に、
ネットワークエラーと認証エラーを区別するロジックを追加してください

# 悪い例: 曖昧
@Claude ログインを修正してください
```

#### コンテキストを活用する

```text
# スレッドを使って詳細なコンテキストを提供
スレッド内のエラーログ・再現手順・チームの議論 → すべてClaudeが参照
```

#### 完了の定義を明確に

```text
@Claude ユーザー検索APIのパフォーマンスを改善してください。
テストを書いて、ドキュメントを更新して、PRを作成するまでが完了です
```

#### Slackとweb版の使い分け

| Slackを使う | web版を直接使う |
|------------|----------------|
| バグ報告やSlackの議論コンテキストが既にある | ファイルをアップロードしたい |
| 非同期でタスクを委譲したい | リアルタイムでやりとりしたい |
| チームに作業の可視性が必要 | 長期・複雑なタスクに取り組む |

### セキュリティの考慮事項

#### コンテキストのセキュリティ

@Claudeを呼び出すと、チャンネルまたはスレッドの会話コンテキストをClaudeが読み取ります：

1. **信頼できるチャンネルでのみ使用**: 外部ユーザーが参加しているチャンネルでは、プロンプトインジェクション攻撃のリスクがあります
2. **機密情報に注意**: パスワードやAPIキーが含まれるスレッドでは使用を避ける
3. **プライベートチャンネルを活用**: 機密性の高いコードベースには専用のプライベートチャンネルを作成する

#### Teams/Enterpriseのセッション共有

TeamsまたはEnterpriseアカウントでは、Slackから作成されたセッションが組織内で自動的に可視化されます。詳細は[Web版Claude Codeの共有設定](https://code.claude.com/docs/en/claude-code-on-the-web#sharing-sessions)を参照してください。

### トラブルシューティング

#### セッションが開始しない

```
確認ポイント:
1. Claude App HomeでClaudeアカウントが接続されているか確認
2. Web版Claude Codeのアクセスが有効か確認
3. GitHubリポジトリが少なくとも1つ接続されているか確認
```

#### リポジトリが表示されない

```
確認ポイント:
1. claude.ai/code でリポジトリを接続する
2. そのリポジトリへのGitHub権限を確認する
3. GitHubアカウントを切断して再接続してみる
```

#### 誤ったリポジトリが選択された

```
対応方法:
1. 「Change Repo」ボタンをクリックして変更
2. リクエストにリポジトリ名を明示して精度を上げる:
   「@Claude myorg/api-serverリポジトリの...」
```

#### 認証エラー

```
対応方法:
1. App HomeでClaudeアカウントを切断して再接続
2. ブラウザで正しいClaudeアカウントにサインインしているか確認
3. ClaudeプランにClaude Codeアクセスが含まれているか確認
```

### 現時点の制限事項

| 制限 | 内容 |
|------|------|
| GitHub限定 | GitLabなど他のGitプラットフォームは未対応 |
| PR作成は1件 | 各セッションで作成できるPRは1つ |
| レート制限 | 個人のClaudeプランのレート制限が適用 |
| Webアクセスが必要 | Web版Claude Codeのアクセスがないユーザーは通常のClaudeチャットのみ利用可能 |
| DMは非対応 | ダイレクトメッセージ（DM）では動作しない（チャンネルのみ） |

## まとめ

- Claude Code in Slackは`@Claude`メンションでコーディングタスクをWeb版Claude Codeセッションに委譲する機能
- ルーティングモードは「Code only」（コーディング専用）と「Code + Chat」（インテリジェント振り分け）の2種類
- スレッドの会話コンテキストを自動収集してバグ報告からPR作成まで一連のフローを自動化できる
- Claudeは招待されたチャンネルでのみ動作し、DMは非対応
- 現在はGitHubリポジトリのみ対応
- 信頼できるチャンネルでのみ使用し、コンテキストのセキュリティに注意する

## 公式リファレンス

- [Claude Code in Slack](https://code.claude.com/docs/en/slack)
- [Claude Code on the web](https://code.claude.com/docs/en/claude-code-on-the-web)
- [Claude for Slack](https://claude.com/claude-and-slack)
- [Slack App Marketplace - Claude](https://slack.com/marketplace/A08SF47R6P4)
